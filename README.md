# raspisator

Солвер для задач целочисленного линейного программирования.

**Все классы МАКСИМИЗИРУЮТ целевую функцию.**

## Becnhmark

В GitHub Actions настроен автоматический запуск солвера на задачах линейного программирования из датасета NETLIB.
Оптимальные значения для сравнения были взяты из статьи "The final NETLIB-LP results".

## Code Style

Стиль кода описан в `.cland-tidy` и `.clang-format`. Однако есть набор дополнительных правил, которые поддерживаются в
проекте.

### Names

- Надо использовать `infeasible`.

### Exceptions

- Каждое сообщение должно быть грамматически полным и корректным предложением. В конце всегда надо ставить точку.
- Для обозначения имён в сообщении об ошибки используются одинарные кавычки. Пример: `Variable 'test' doesn't exist.`
- Сообщение об ошибке должно говорить о том, что сломалось, а не о том, что должно быть выполнено. Например, надо
  использовать "Initial point is not primal feasible." вместо "Initial point must be primal feasible."

## Random Generators

Генераторы случайных чисел надо принимать по ссылке. Пример:

```c++
template <typename Gen>
  requires std::uniform_random_bit_generator<Gen>
size_t random_number(Gen& random);
```

### Tests

- Имена тестов должны быть в CamelCase и не должны включать в себя Test

## Мысли

Тут собирается то, что пока что не полностью оформлено в коде и тестах, но сидит в моей голове.

- симплекс после завершения должен оставаться в том базисе, результат для которого он вернул. Человек может попросить
  вернуть какие-то характеристики этого базиса (например строку из приведённой таблицы).
